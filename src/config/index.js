const CONFIG = {
    MAX_COORDINATE_LENGTH: 50,
    DIRECTIONS: [
        "N", "S", "E", "W"
    ],
    INSTRUCTIONS: [
        "L", "R", "F"
    ],
    PATH: {
        "marsapi": "/mars-robot"
    }
};

export default CONFIG;